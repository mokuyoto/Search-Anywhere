document.addEventListener('mouseup', function() {    // EVENT LISTENER FOR TEXT HIGHLIGHTING
    const selectedText = window.getSelection().toString();
    // logic for creating and displaying suggestion box, + position box above the selected text....
});


function createSuggestionBox(selectedText) {
    // HTML ELEMENTS....
}

function handleSearch(option) { // handle search option clicks...
    // redirect to url.///
}